<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Schrottwichteln – Anmeldung</title>
  <style>
    :root {
      --bg: #0b0f14;
      --card: #121821;
      --text: #e8edf2;
      --muted: #9fb0c3;
      --accent: #7cc5ff;
      --accent-2: #8cffc7;
      --ring: rgba(124,197,255,.5);
      --error: #ff7272;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
      background: radial-gradient(1200px 1200px at 90% -10%, rgba(124,197,255,.12), transparent 60%),
                  radial-gradient(1000px 900px at -10% 110%, rgba(140,255,199,.10), transparent 60%),
                  var(--bg);
      color: var(--text);
      min-height: 100svh; display: grid; place-items: center; padding: 24px;
    }
    .card {
      width: 100%; max-width: 640px; background: linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
      border: 1px solid rgba(255,255,255,.08); border-radius: 18px; backdrop-filter: blur(8px);
      box-shadow: 0 10px 35px rgba(0,0,0,.35);
    }
    .inner { padding: 28px; }
    h1 { font-size: clamp(1.6rem, 2.8vw, 2.2rem); margin: 0 0 8px; letter-spacing: .3px; }
    .badge { display: inline-flex; align-items: center; gap: .5ch; font-size: .82rem; color: var(--muted);
      background: rgba(255,255,255,.04); border: 1px solid rgba(255,255,255,.08); padding: 6px 10px; border-radius: 999px;
    }
    p.desc { margin: 14px 0 22px; color: var(--muted); line-height: 1.55; }

    form { display: grid; gap: 14px; margin-top: 8px; }
    .field { display: grid; gap: 8px; }
    label { font-size: .95rem; color: var(--text); }
    .input {
      width: 100%; padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,.12);
      background: linear-gradient(180deg, rgba(10,14,18,.85), rgba(14,20,26,.85));
      color: var(--text); outline: none; transition: border-color .2s, box-shadow .2s;
    }
    .input::placeholder { color: #7f90a3; }
    .input:focus { border-color: var(--accent); box-shadow: 0 0 0 4px var(--ring); }

    .row { display: grid; gap: 14px; grid-template-columns: 1fr; }
    @media (min-width: 560px) { .row { grid-template-columns: 1fr 1fr; } }

    .hint { font-size: .85rem; color: var(--muted); }

    button {
      appearance: none; border: 0; cursor: pointer; font-weight: 600; letter-spacing: .2px;
      padding: 12px 16px; border-radius: 12px; color: #ffffff; background:
        rgba(123,14,7,.5);
      box-shadow: 0 8px 20px rgba(123,14,7,.35), inset 0 0 0 1px rgba(123,14,7,.35);
      transition: transform .04s ease, filter .2s ease;
    }
    button:hover { filter: brightness(1.05); }
    button:active { transform: translateY(1px); }

    .note { display: flex; align-items: center; gap: 10px; font-size: .88rem; color: var(--muted); margin-top: 8px; }
    .note svg { opacity: .8; }

    .footer { padding: 16px 28px; border-top: 1px solid rgba(255,255,255,.08);
      display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 10px;
    }
    .brand { display: flex; align-items: center; gap: 10px; }
    .logo 
    .brand b { letter-spacing: .3px; }
    .legal { color: var(--muted); font-size: .82rem; }

    .error { color: var(--error); font-size: .9rem; display: none; }
    .success { color: var(--accent-2); font-size: .95rem; display: none; }
  </style>
</head>
<body>
  <main class="card" role="region" aria-labelledby="title">
    <div class="inner">
      <span class="badge" aria-hidden="true">🎄 Weihnachtesevent 🎄</span>
      <h1 id="title">"Schrott" Wichteln</h1>
      <p class="desc">
        Willkommen beim diesjährigen Schrottwichteln! Bringe einen originellen <b>Schatz</b> aus dem Keller mit und freu dich auf
        überraschende Tauschpartner. 
      </p>
      <p class="description">
        Interesse geweckt? Dann trag dich hier kurz mit Namen und E‑Mail ein, damit wir dir alle nötigen Infos schicken können. 
      </p>

     <form id="signup" action="https://formspree.io/f/mwprnkeo" method="POST">
     <input type="hidden" name="_subject" value="Neue Schrottwichtel-Anmeldung">
        <div class="row">
          <div class="field">
            <label for="name">Dein Name</label>
            <input class="input" id="name" name="name" type="text" placeholder="z. B. Santa Claus" required minlength="2" />
          </div>
          <div class="field">
            <label for="email">E‑Mail</label>
            <input class="input" id="email" name="email" type="email" placeholder="z. B. santa@nordpol.com" required />
          </div>
        </div>

        <button type="submit">Anmelden</button>
        <p id="err" class="error" role="alert">Bitte überprüfe deine Eingaben.</p>
        <p id="ok" class="success" role="status">Danke! Deine Anmeldung wurde erfasst.</p>
        <p class="note" aria-live="polite">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M12 2L2 7l10 5 10-5-10-5Zm0 7L2 4v13l10 5 10-5V4l-10 5Z" stroke="currentColor" stroke-width="1.2"/>
          </svg>
          Wir senden nur Event‑Infos. Kein Spam, kein Newsletter.
        </p>
      </form>
    </div>
    <div class="footer">
      <div class="brand">🎁<div aria-hidden="true"></div><b>Schrottwichteln 2025</b></div>
      <div class="legal">Mit der Anmeldung stimmst du unserer freundlichen Organisationsbedingungen zu. 😊</div>
    </div>
  </main>

  <script>
    const form = document.getElementById('signup');
    const nameEl = document.getElementById('name');
    const mailEl = document.getElementById('email');
    const err = document.getElementById('err');
    const ok = document.getElementById('ok');

    function isValidEmail(v){
      return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v);
    }

    form.addEventListener('submit', (e) => {
      e.preventDefault();
      err.style.display = 'none'; ok.style.display = 'none';

      const valid = nameEl.value.trim().length >= 2 && isValidEmail(mailEl.value.trim());
      if(!valid){ err.style.display = 'block'; return; }

      // TODO: Replace with real submit (fetch to your backend or service)
      // Example for later:
      // fetch('/anmelden', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({ name: nameEl.value, email: mailEl.value }) })
      //   .then(r => r.ok ? ok.style.display = 'block' : (err.style.display = 'block'))
      //   .catch(() => err.style.display = 'block');

      ok.style.display = 'block';
      form.reset();
    });
  </script>
</body>
</html>
